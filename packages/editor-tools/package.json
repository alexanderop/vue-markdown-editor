{
  "name": "@vme/editor-tools",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint . --fix && eslint . --fix --cache",
    "format": "oxfmt src/",
    "test": "vitest run --config vitest.config.ts"
  }
}
